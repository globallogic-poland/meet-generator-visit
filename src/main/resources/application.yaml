server:
  port: 30100

spring:
  application:
    name: generator-visit

  cloud:
    stream:
      bindings:

        visits-out:
          destination: visits
          contentType: application/json

        input:
          destination: visits